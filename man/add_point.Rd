% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_point.R
\name{add_point}
\alias{add_point}
\title{Add a single point marker in exploratory mode}
\usage{
add_point(
  map,
  lng,
  lat,
  popup = "",
  color = "dodgerblue",
  group = "New - Exploring"
)
}
\arguments{
\item{map}{A Leaflet map object, as returned by [leaflet::leaflet()] or
piped through other `leaflet` layer functions.}

\item{lng}{Numeric. Longitude of the point in decimal degrees.}

\item{lat}{Numeric. Latitude of the point in decimal degrees.}

\item{popup}{Character. HTML string displayed in the marker's popup on click.
Defaults to `""` (no popup).}

\item{color}{Character. Fill color of the circle marker, as a CSS color name
or hex string. Defaults to `"dodgerblue"`.}

\item{group}{Character. Name of the layer group the marker is assigned to,
used with [leaflet::addLayersControl()] for toggling. Defaults to
`"New - Exploring"`.}
}
\value{
The modified Leaflet map object (invisibly), with the circle marker
  added. Suitable for further piping.
}
\description{
A thin wrapper around [leaflet::addCircleMarkers()] that adds a styled
circle marker to a Leaflet map with sensible defaults for interactive
exploration.
}
\examples{
leaflet::leaflet() |>
  leaflet::addTiles() |>
  add_point(lng = -76.5, lat = 42.4, popup = "Test site")
}
\seealso{
[leaflet::addCircleMarkers()]
}
