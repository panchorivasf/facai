% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rm_except_script.R
\name{rm_except_script}
\alias{rm_except_script}
\title{Remove objects not defined in a script from the R environment}
\usage{
rm_except_script(script_path, envir = .GlobalEnv)
}
\arguments{
\item{script_path}{Character. Path to the R script file to parse.}

\item{envir}{Environment from which to remove objects. Defaults to
\code{.GlobalEnv}.}
}
\value{
Invisibly returns a character vector of the removed object names.
}
\description{
Parses a script file for assignment expressions (\code{<-}) and removes all
objects from the specified environment whose names do \emph{not} appear in
the script. Useful for isolating only the outputs of a given script while
clearing unrelated objects. Pair with \code{\link{this_script}} to
self-reference the active script.
}
\note{
Only detects assignments made with \code{<-}. Objects created with
  \code{=} or \code{assign()} will not be detected and will be treated as
  non-script objects, making them candidates for removal.
}
\examples{
\dontrun{
rm_except_script("scripts/ituri_formatting.R")
rm_except_script(this_script())
}

}
\seealso{
\code{\link{rm_from_script}}, \code{\link{this_script}}
}
